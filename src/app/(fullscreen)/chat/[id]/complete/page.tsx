import { ChatCompleteSelector } from '@components/chat/chat-complete-selector';

/** CHAT-004 교환 완료 포카 선택 (id = 서버 chatId) */
export default function ChatCompletePage() {
  return <ChatCompleteSelector />;
}
